<%- include("../partials/header") %>

<div class="signup-container">
  <form action="/move/register" method="POST" class="signup-form" id="signup-form">
    <input type="hidden" name="_csrf" value="<%= csrfToken %>">
    <h1><i class="fas fa-user-plus"></i> Sign Up</h1>
    <p class="info-text">Create an account to start organizing your move</p>
    <hr />

    <div class="input-container">
      <label for="name"><i class="fas fa-user"></i> Name</label>
      <input type="text" placeholder="Enter your name" id="name" name="name" required />
    </div>

    <div class="input-container">
      <label for="email"><i class="fas fa-envelope"></i> Email</label>
      <input type="email" placeholder="Enter your email" id="email" name="email" required />
    </div>

    <div class="input-container">
      <label for="psw"><i class="fas fa-lock"></i> Password</label>
      <input type="password" placeholder="Create a password" id="psw" name="psw" required />
    </div>

    <div class="input-container">
      <label for="psw_repeat"><i class="fas fa-check-circle"></i> Confirm Password</label>
      <input type="password" placeholder="Repeat your password" id="psw_repeat" name="psw_repeat" required />
    </div>

    <% if (errorMessage) { %>
    <div class="error-message" id="error-message">
      <p><i class="fas fa-exclamation-triangle"></i> <%= errorMessage %></p>
    </div>
    <% } %>

    <button type="submit" class="signupbtn" id="submit-button"><i class="fas fa-rocket"></i> Sign Up</button>

    <div class="login-link" id="login-link">
      <p>Already have an account? <a href="/move/login"><i class="fas fa-sign-in-alt"></i> Login here</a></p>
    </div>
  </form>
</div>

<%- include("../partials/footer") %>

<script nonce="<%= cspNonce %>">
  document.addEventListener("DOMContentLoaded", () => {
    const errorMessage = document.getElementById("error-message");
    const submitButton = document.getElementById("submit-button");
    const loginLink = document.getElementById("login-link");

    if (errorMessage) {
      submitButton.style.display = "none";
      loginLink.style.display = "none";
      errorMessage.style.display = "block";
      setTimeout(() => {
        errorMessage.style.display = "none";
        submitButton.style.display = "block";
        loginLink.style.display = "block";
      }, 4000);
    }
  });
</script>
