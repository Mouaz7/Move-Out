<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %> - Box Details</title>
    <link rel="stylesheet" href="/style/style.css" />
  </head>
  <body>
    <%- include("../partials/header") %>

    <div class="box-details-page">
      <div class="box-details-card">
        <div class="box-details-header">
          <h1><%= box.box_name %></h1>
          <span class="privacy-badge <%= box.is_private === 1 ? 'private' : 'public' %>">
            <%= box.is_private === 1 ? "Private" : "Public" %>
          </span>
        </div>

        <% if (box.label_image && box.label_image !== 'undefined') { %>
        <div class="label-image-section">
          <img src="/style/images/<%= box.label_image %>" alt="Label Image" class="label-hero-image" />
          <% if (box.label_name) { %>
          <p class="label-caption"><%= box.label_name %></p>
          <% } %>
        </div>
        <% } %>

        <div class="box-info-list">
          <div class="info-row">
            <span class="info-row-label">Content Type</span>
            <span class="info-row-value"><%= box.content_type %></span>
          </div>
          <% if (box.label_name) { %>
          <div class="info-row">
            <span class="info-row-label">Label</span>
            <span class="info-row-value"><%= box.label_name %></span>
          </div>
          <% } %>
        </div>

        <div class="content-section">
          <p class="content-section-title">Content</p>
          <div class="content-box">
            <% if (box.content_type === 'text') { %>
            <p><%= box.content_data %></p>
            <% } else if (box.content_type === 'image') { %>
            <img src="/uploads/<%= box.content_data %>" alt="Box Image" />
            <% } else if (box.content_type === 'audio') { %>
            <audio controls>
              <source src="/uploads/<%= box.content_data %>" type="audio/mpeg" />
              Your browser does not support the audio element.
            </audio>
            <% } else { %>
            <p>No content available to display.</p>
            <% } %>
          </div>
        </div>
      </div>
    </div>

    <%- include("../partials/footer") %>
  </body>
</html>
